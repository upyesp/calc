[package]
name = "calc-tui"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Native full-screen terminal frontend."

[[bin]]
name = "calc-tui"
path = "src/main.rs"

[dependencies]
calc-core = { path = "../core" }
calc-store = { path = "../store", features = ["fs"] }
ratatui = "0.29"
crossterm = "0.28"
